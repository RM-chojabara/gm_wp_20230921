フォルダ内のSCSSは、VSCODE の Live SASS を使用してコンパイルしてます。
https://marketplace.visualstudio.com/items?itemName=glenn2223.live-sass

```
	"liveSassCompile.settings.formats": [
		{
			"format": "expanded",
			"extensionName": ".css",
			// 保存先の指定
			"savePath": "./"
		}
	],

```